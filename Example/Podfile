source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'SwiftPhoenixClient_Example' do
  inherit! :search_paths
  pod "SwiftPhoenixClient", :path => "../"
end

target 'SwiftPhoenixClient_Tests' do
  inherit! :search_paths
  pod "SwiftPhoenixClient", :path => "../"  
end
